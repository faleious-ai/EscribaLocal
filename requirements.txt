# Dependências principais — pinadas nas versões instaladas e validadas em 2026-06-10.
# Atualizações de versão são decisão explícita (ver painel de ambiente, Lote 8),
# nunca automáticas — especialmente transformers/bitsandbytes/accelerate, que
# interagem com o fork vendored em custom_transformers/.
#
# IMPORTANTE: torch NÃO fica neste arquivo de propósito. Ele é instalado pelo
# iniciar.bat com o índice CUDA (torch==2.5.1+cu121 via
# https://download.pytorch.org/whl/cu121); listá-lo aqui faria o pip
# substituí-lo pela build CPU do PyPI.
fastapi==0.136.3
uvicorn==0.49.0
faster-whisper==1.2.1
psutil==7.2.2
python-multipart==0.0.30
soundfile==0.13.1
imageio-ffmpeg==0.6.0
websockets==16.0
transformers==5.10.2
bitsandbytes==0.49.2
accelerate==1.13.0
librosa==0.11.0
scipy==1.17.1
huggingface_hub==1.18.0
# Monitor de GPU sem subprocess (usado a partir do Lote 5)
nvidia-ml-py==13.610.43
pywin32==312; platform_system == "Windows"
